refactor: 优化首页及物品库页面设计还原与底栏 TabBar 规范化

- 首页 (pages/index/index.vue)：
  1. 将“今日关注”由横向滚动重构为双列网格布局，微调卡片边框、背景及角标样式
  2. 优化分类网格布局及间距，新增分类图标圆底容器并微调尺寸
  3. 规范化悬浮按钮 (FAB) 阴影与定位
- 物品库页 (pages/library/index.vue)：
  1. 优化顶部栏阴影、滚动主体高度及空状态布局，引入新版空状态插画
  2. 重构物品卡片状态展示，使用 SVG 状态图标替代文本符号，对过期物品增加名称删除线
- 底部导航栏 (TabBar) 规范化：
  1. 重构并统一首页与物品库页的自定义 TabBar 样式
  2. 按照规范引入 68rpx 的 icon-wrap 容器，实现激活态背景色与图标物理居中，文字激活态加粗
  3. 引入底部安全区域适配 (safe-area-inset-bottom)
- 其它更新：
  1. 调整 manifest.json、.agents/agents.md 配置及 logo 静态图片
  2. 新增物品库空状态图片资源 (library-empty-image.png)

影响范围：首页、物品库页面的视觉设计还原与底栏基础交互
