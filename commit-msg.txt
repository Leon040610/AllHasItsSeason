refactor: 精细化数据管理与我的模块子页面 UI 还原

- 导航栏及高度适配优化 (各子页面)：
  1. 为“关于”、“数据管理”各子页以及“草稿箱”统一配置顶栏返回键为 `data-not-logged-fanhui.svg` 图标，去除硬编码的文本字符
  2. 修复滚动容器的顶部内边距，统一累加 `var(--status-bar-height, 44rpx)` 以适配刘海屏及状态栏高度
- 数据管理 - 未登录页 (pages/me/data/not-logged.vue)：
  1. 将设置列表拆分为独立的 `settings-group` 卡片组，并微调高度至 144rpx
  2. 将跳转箭头替换为规范的 `data-not-logged-xuanze.svg` 图标
- 数据管理 - 同步中页 (pages/me/data/syncing.vue)：
  1. 优化进行中进度条样式，为其增加左侧深绿指示边条 (::before)
- 其它细节：
  1. 统一各子页面的全局字体为 Noto Serif SC

影响范围：我的模块下所有二级页面的头部导航与内容排版
