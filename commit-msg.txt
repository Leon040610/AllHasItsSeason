feat: 新增草稿箱批量清理与脏表单存草稿挽留机制并优化逻辑

- 草稿箱模块 (pages/me/draft/index.vue & services/draftService.js)：
  1. 新增批量管理模式，支持多选草稿并进行一键“批量清理”，优化批量删除与单项删除的确认弹窗与交互文案
  2. 优化 `computeMissingFields` 缺失项判定逻辑，根据不同的效期模式 (常态/开封/双重) 精确提示具体缺失的属性（如“待确认到期日”、“待补开封效期”），使缺失项说明更精确温和
  3. 规范化草稿的数据源字段映射
- 添加与编辑模块 (pages/add/ & pages/detail/edit/)：
  1. 重构添加物品页的脏数据检测方法 `isFormDirty`，使用初始状态的 JSON 快照与当前数据进行深度比对
  2. 实现脏表单返回时的“存草稿挽留弹窗”（先留一份草稿吗？），支持一键“存草稿”并安全退出，防止用户录入数据意外丢失
  3. 优化编辑详情页未保存返回时的挽留提示文案（还没保存修改）
- 数据同步模块 (pages/me/data/success.vue)：
  1. 修复同步成功页缺失的 `<script setup lang="ts">` 声明，解决 Vue 编译警告

影响范围：草稿箱列表管理、物品添加与编辑流程、数据同步模块
