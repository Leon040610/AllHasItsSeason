feat: 新增同步日志页面并实现同步预检与中断机制

- 同步控制增强 (services/syncService.js & repositories/wechatCloudSyncRepository.js)：
  1. 新增 `preflightSync()` 预检机制，支持在正式同步开始前拉取云端统计并与本地数据作差异比对
  2. 新增 `getSyncLogs()` 接口用于拉取云端历史同步日志
  3. 新增同步暂停/中断 (cancelRequested) 状态流控，在多集合循环分批拉取和推送之间执行中断检查，确保能优雅中止同步并上传 `cancelled` 状态日志
- 云端日志与预检测分支 (cloudfunctions/syncData/index.js)：
  1. 升级云函数，增加 `getLogs`（查询历史同步日志，倒序限制10条）与 `preflight`（统计并返回用户云端各表数据行数）处理分支
  2. 兼容处理 `cancelled` 同步状态的入库记录
- 同步日志展示与交互 (pages/me/data/logs.vue & pages.json)：
  1. 新增同步日志页 (pages/me/data/logs.vue)，以时间卡片排版显示每次同步的耗时、完成件数、冲突数及失败描述，并完成路由注册
  2. 数据管理主页 (index.vue) 新增“同步日志”跳转入口项
  3. 优化同步中页 (syncing.vue) 交互，结合多集合进度拆解，支持用户中途点击“取消”安全中止同步
- 其它细节：
  1. 修正部分页面在不同同步状态下的 CSS 细节问题

影响范围：微信云同步数据控制层、数据管理日志查看页及同步状态管理器
