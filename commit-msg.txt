feat: 实现真机自定义字体动态加载与“我的”模块 UI 精细化还原

- 字体加载优化 (App.vue & static/fonts/)：
  1. 引入并上传思源宋体子集字体文件 (noto-serif-sc-subset.woff2)
  2. 重构 App.vue，实现真机动态加载自定义字体流程（优先从云存储下载加载，若无则上传本地文件并加载）
- “我的”模块重构 (pages/me/ 目录各页面)：
  1. 我的主页 (me/index.vue) 引入用户区及卡片区的背景发光微动效
  2. 用户头像尺寸增大，外包白边与弥散阴影，使其更具手帐贴纸质感
  3. 规范化设置项列表，为图标增加 `settings-item__icon-wrap` 容器，替换文本箭头为 `me-tiaozhuan.svg` 图标
  4. 规范化该模块下所有子页面（关于我们、数据管理、草稿箱、提醒设置）的图标文件命名，由原 `calculator-` 前缀变更为 `me-` 前缀，并清理旧图标文件
- 其它修改：
  1. 首页与物品库微调 TabBar 选中态的图标显示效果
  2. 包含规划与进度文档记录 (task_plan.md, progress.md, findings.md)

影响范围：全站真机字体渲染、“我的”页面及子页面的视觉布局与静态资源
