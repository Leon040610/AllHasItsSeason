feat: 扩展云同步服务以支持多数据集合 (物品/草稿/分类/提醒/设置) 同步

- 云端多集合同步机制 (cloudfunctions/syncData/ & repositories/wechatCloudSyncRepository.js)：
  1. 重构 `syncData` 云函数，增设 `collection` 集合参数，支持对 `items`、`drafts`、`categories`、`reminder_settings`、`sync_settings` 进行统一的安全隔离同步
  2. 设计并实现 `extractSafePayload` 函数，根据不同的集合策略抽取安全的白名单字段写入数据库
  3. 修改同步的时间戳字段类型为 Unix 毫秒时间戳，排除异地环境时区对冲突合并判定的影响
  4. 重构 `wechatCloudSyncRepository` 相关的网络 I/O 接口为通用的 `pullData`/`upsertData` 方法，同时提供老 API 兼容函数
- 核心同步控制升级 (services/syncService.js)：
  1. 重构 `syncItems()`，基于可配置的数据源列表循环执行多集合拉取与推送合并，并在同步完成后回写本地缓存
  2. 引入 `operationId` 统一识别一次同步动作，并细化聚合上报 `collectionStats` 分类数据统计日志
- 服务与页面逻辑对接：
  1. 升级分类、草稿、提醒及设置等服务层以支持同步字段适配及 `applySyncResult` 本地合并操作
  2. 修复 `utils/dateUtils.js` 中毫秒时间戳转换兼容及多处界面状态的展示判定

影响范围：云开发同步链路、全站各类数据的持久化与合并合并策略、数据同步模块 UI
